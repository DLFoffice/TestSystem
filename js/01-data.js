/* ============================================================
   01-data.js — ข้อมูลนักเรียนตั้งต้น (RAW) — จะถูกแทนที่ด้วย Firestore ในขั้นถัดไป
   (แยกมาจาก index.html เดิม บรรทัด 1399-1402 โดยรักษาลำดับโค้ดเดิม)
   ============================================================ */

// ============ DATA (รูปภาพทั้งหมดถูกลบออก photoUrl เป็นค่าว่าง) ============
const RAW = [];
// Initialize DB with all required fields
